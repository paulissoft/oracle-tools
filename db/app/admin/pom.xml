<?xml version="1.0" encoding="windows-1252" ?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  
  <parent>
    <groupId>com.paulissoft.pato.db</groupId>
    <artifactId>app</artifactId>
    <version>${revision}</version>
  </parent>

  <modelVersion>4.0.0</modelVersion>
  <groupId>com.paulissoft.pato.db.app</groupId>
  <artifactId>ADMIN</artifactId>
  <packaging>pom</packaging>
  <description>Build ADMIN component</description>
  
  <properties>
    <flyway.table>schema_version_tools_admin</flyway.table>
    <db.src.callbacks.scripts>${db.src.scripts}/callbacks</db.src.callbacks.scripts>
    <db.proxy.username></db.proxy.username>
    <db.schema>${project.artifactId}</db.schema>
    <db.username>${db.schema}</db.username>
    <db.object.names.include>1</db.object.names.include>
    <db.object.names>
      ADMIN_INSTALL_PKG,
      ADMIN_RECOMPILE_PKG,
      ADMIN_SCHEDULER_PKG,
      ADMIN_SYSTEM_PKG,
    </db.object.names>
  </properties>

  <build>
    <resources />
  </build>

</project>

